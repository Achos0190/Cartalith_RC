# Roadmap

Priority-ordered. One thing at a time; each item ends with `tests/run.sh` green and a browser sanity check. Detailed designs live in `docs/research/` and `docs/UNIFIED_TOOL_PLAN.md`.

## Now

User-set sequence (June 2026): **visuals → 16k tiling → gravity (G2/G3) → then a river-painting / stream-carving quality pass**.

1. **Visuals, asset tier** (`docs/BIOME_AND_VISUALS_PLAN.md`): zero-asset procedural tier complete (B1 parchment + B3 icons v0.050, B4 waves v0.051). **B2 texture splatting + B3 sprite icons are gated on user approval of a CC0 pack — candidate shortlist ready in `docs/research/asset-candidates.md`** (ambientCG textures + K.M. Alexander #NoBadMaps sprites). Once a subset is approved: vendor into `assets/` + wire splatting/sprites with procedural fallback. Optional: B4 vector spline coastlines.
2. **World→regional→16k tiling pipeline** (`docs/WORLD_REGIONAL_TILING_PLAN.md`): pure core in v0.052, browser wiring in v0.053 (region-select drag + `exportRegionTiles`: per-tile refine→rg16→gzip(CompressionStream)→PNG(OffscreenCanvas)→manifest-v2 ZIP). Remaining nice-to-haves: per-tile *erosion* at refine time (worker kernels exist), fflate vendoring for speed, a 16k 4×4@4096 memory test on a real device — all post-manual-pass.
3. **Gravity completion**: **G2** geoid sea-level field DONE in v0.054 (`buildGeoid` J2 bulge + harmonics + mantle fbm; local sea level threads through water mask/climate/erosion/render; off ⇒ bit-identical). **G3** moons & tidal-range field SHIPPED v0.070 (gated, bit-identical; Tides debug view + intertidal overlay + `tidal_range.f32` export). **G4** tidal sedimentation SHIPPED v0.098 (`applyTidalSedimentation`/Tidal-flats button). **Gravity workstream G1–G4 complete.**
4. **River / stream-carving quality pass = Earth-system coupling** (`docs/research/system-coupling-audit.md`, user picked loops 1/2/3): **all three SHIPPED**: L1 climate↔erosion `evolveCoupled` (v0.066), L2 currents→winds (v0.067), L3 mass-conserving sediment routing `routeSediment`/Sediment-fill (v0.069). **L6 cryosphere albedo SHIPPED (v0.091).** Item 4 complete. **L4 dynamic lithology SHIPPED v0.098** (`recomputeResistanceAfterErosion`, exhumation hardening). All prioritised + optional audit loops done.

## Next

5. **P0–P1 — Unified tool shell merge** (`docs/UNIFIED_TOOL_PLAN.md`): namespace engine under `Gen`, merge into `cartalith_gen1_v0.001.html`, 5-tab UI, layers panel. **(The Atlas workstream (Phases 1–4) and the R-series rendering framework (R1–R4) are now complete — v0.086/v0.089.)**
6. **P2 — Save schema v10** with both legacy importers.
7. **W0b — remaining micro-opts** (`docs/research/engine-optimization.md`): JS micro-opts in the stencil loops — only if profiling shows a need.

## Later

10. **P3 — Climate→content bridges**: paint-grid fill from climate, flowField→ways river tracing, climate-aware planner, salt-flat/endorheic material (handoff pending #6).
13. ~~**R32F GPU migration**~~ — SHIPPED v0.099 (R32F single-channel when renderable, RGBA32F fallback; 4× texture memory). Browser pass owed.
14. **Disturbance model** — wind-throw (W1 wind × canopy × slope) + flood proxy (TWI + discharge) SHIPPED v0.098 as read-only debug views. Further work (optional): bake hazard layers to export, drive content placement.
15. ~~**GUI overhaul**~~ — SHIPPED v0.100 (user request): header **Import ▾** menu, dedicated **Tiles & LOD** section with live export-size estimate + on-map tile-border preview, Erosion/Debug accordions, merged Calibrate→Scale, slimmed export section. UI-only ⇒ bit-identical. Browser pass owed.

(G2 geoid and G3 moons/tides moved up into Now → item 3, per the user's June 2026 sequencing.)

## Bigger workstreams (planned)

- **Tectonic feature graph** (`docs/research/tectonic-feature-graph.md`, user proposal June 2026 — **T0–T5 COMPLETE**): shear field + boundary matrix (v0.058) → polyline graph (v0.060) → orogenic kernel (v0.061) → per-type profiles: trench+arc, collision belts, rift grabens (v0.062) → transform faults (v0.064) → T5 archetype tuning (fold-intensity/trench-depth sliders + archetype hooks, v0.090).

- **Terrain rendering enhancement** (`docs/research/terrain-rendering-enhancement.md`, user research June 2026): multi-scale hillshading → AO → slope-material refinement → ridge crest highlights → three-frequency texture synthesis → minor-channel flow lines → elevation-weighted ridged noise formalisation. Phased R1–R4, all render-only/gated at defaults. Approach: painterly cartographic relief without additional geological simulation; seamless at >16k via world-coordinate noise. Targets visual quality parity with reference atlas maps and World Machine/Gaea outputs.

## Bigger workstreams (planned, post-merge)

- **Biome handoff + visuals** (`docs/BIOME_AND_VISUALS_PLAN.md`): dual raster + 14-index paint grid with lock flags → procedural realistic base (parchment grain) → texture splatting (CC0 pack) → togglable Nortantis-style icon mountains.
- **World/regional tiling to 16k** (`docs/WORLD_REGIONAL_TILING_PLAN.md`): coarse world → region select → world-space-seeded amplified tiles with skirts → per-tile OffscreenCanvas/worker → fflate-compressed tiled export + manifest; 16-bit height packing & external 16-bit/.f32 import.

## Research / spikes

- **Terrain rendering enhancement** (June 2026): `docs/research/terrain-rendering-enhancement.md` — framework for multi-scale shading, AO, ridge synthesis, slope-material masks, flow-line enhancement, crest highlights, procedural texture synthesis. Phased plan R1–R4.
- Map-painting & zoom UX (Wonderdraft / World Machine / World Creator takeaways): `docs/research/map-painting-ux.md` → shipped as v0.048 (plotline feature brushes, zoom/pan + scale bar).
- Rust/WASM SIMD erosion kernel (inline base64) — only if profiling after #3/#8 still shows CPU-bound erosion. Verdict & sources: `docs/research/engine-optimization.md`.
- WebGPU compute backend — Gen1 v2 candidate.
- Dirty-rect rendering (handoff pending #5) — when brush latency becomes a complaint.

## Done

- 2026-06: **v0.099 — R32F GPU texture migration** (GPU memory). The WebGL2 compute path already used RGBA32F full-float, so this is a 4× texture memory/bandwidth win (16→4 B/px), not precision. `GPU.init` probes R32F color-renderability (`_probeR32F`) and uses `R32F`/`RED` single-channel textures + uploads/readbacks when available, else the proven RGBA32F path; no fragment-shader changes (shaders already use only `.r`); falls back to RGBA32F if the R32F self-test fails before disabling the GPU. CPU paths untouched ⇒ 514 green, field/temp/render byte-identical to v0.098. **GPU path needs a manual browser pass** (headless = CPU fallback only). Completes the "finish everything except the merge" push (v0.097 SDF · v0.098 physical tails · v0.099 R32F).
- 2026-06: **v0.098 — physical-model tails (G4 · L4 · disturbance)** (`docs/research/physical-model-tails.md`). Three opt-in/gated/debug-only additions ⇒ default generate+render bit-identical to v0.097. **G4** (completes gravity): `applyTidalSedimentation` accretes intertidal mudflats toward sea level; opt-in **Tidal flats** erosion button (needs Tides). **L4**: `recomputeResistanceAfterErosion` hardens deeply-exhumed rock (`state.tect.dynamicLithology`, default off) → differential erosion over Evolve cycles. **Disturbance**: read-only **Wind-throw** + **Flood** hazard debug views (`buildWindThrowField`/`buildFloodField`, wind×canopy×slope / TWI+discharge+base-level). 514 green (+15). Second item of the "finish everything except the merge" push. Browser pass owed.
- 2026-06: **v0.097 — SDF finish (bakes + LOD tiles + AA + JFA)** (`docs/research/sdf-control-fields.md`). Closes the v0.096 deferred items: coast/river tints + biome `ecoK` factored into shared `applyCoastRiverSDFv`/`sdfEcoKv` used by both `surfaceColor` and `bakePixel` (SDF now bakes into PNGs); `renderBiomeTileRGBA` reconstructs local river + biome SDFs per tile; sub-pixel land/water AA via `smoothstep(_coastSDF)`; new `jfaDist` (true-Euclidean Jump Flooding, generalized from `assignPlates`) as the SDF backend via `opts.euclid` (chamfer stays default + wave path). All render-only/opt-in ⇒ off bit-identical to v0.096 (main-map, bakePixel, LOD-tile hashes byte-identical). 500 green (+9). Browser pass owed. First item of the "finish everything except the merge" push.
- 2026-06: **v0.096 — SDF geometric control fields (coast + rivers + biomes)** (`docs/research/sdf-control-fields.md`). Generalizes the unsigned `computeCoastDistance` into pure primitives: `chamferDist` (shared 2-pass engine), `buildCoastSDF` (signed: <0 inland / 0 shore / >0 offshore), `buildRiverSDF` (signed channel distance), `buildBiomeBoundaryDist` (ecotone half-width). Three Style sliders (default 0 ⇒ bit-identical): **coast** → constant-world-width shore-sand + coastal-plain bands; **rivers** → bank/wetland/floodplain margins; **biomes** → distance-proportional ecotones (via a new default-1 `ecoK` param on `landColorCore`). **B5 reverse-mipmap**: `renderBiomeTileRGBA` reconstructs coastlines from a local per-tile SDF so they hold constant real-world width at any zoom. Render-only; off ⇒ main-map AND LOD tile render hashes both byte-identical to v0.095. 491 green (+18). Chamfer is "good enough"; JFA (`assignPlates`) is the noted Euclidean upgrade. Browser pass owed (coast/river/biome aesthetics, LOD constant-width coasts). Deferred: river/biome SDF in tiles, SDF in PNG bakes, sub-pixel land/water AA.
- 2026-06: **v0.095 — multi-scale rivers Phase 2/3 (workstream complete)** (`docs/research/multiscale-rivers.md`). Two pure LOD-only additions on top of v0.094's `burnChannels`. **Phase 2 micro-erosion**: `tileMicroErodeKernel(fld,W,H,P,fixed)` (self-contained, worker-ready) runs a small droplet pass on an amplified tile for natural terracing/meanders; the `fixed` border-ring mask keeps the tile edge byte-identical (seam-Δ=0). `tileErode` wrapper + a **"Micro-erode tiles"** LOD toggle (`_lodMicroErode`, default off, slower). **Phase 3 delta sharpening**: `sharpDelta(...)` carves coarse-flow local-maxima channels (dominant distributaries, Bolla Pittaluga 70/30) an extra `sharpK` deeper in the delta zone (`tile<sea+zoneH`) for channel-vs-floodplain contrast; the local-max test runs in coarse-cell space → seam-safe; rides the existing "Burn river channels" toggle. Both wired into `pyramidTile`. Off ⇒ bit-identical to v0.094 (field+temp checksums identical cross-version). 473 green (+12). Browser check: toggle both, Refine a delta → micro-eroded carved channels + distinct distributaries.
- 2026-06: **v0.094 — AGREE multi-scale river burning** (`docs/research/multiscale-rivers.md` Phase 1). Pure `burnChannels(tile,W,H,coarseFlow,cW,cH,bounds,sea,opts)` — AGREE-style channel conditioning applied to amplified LOD tiles. Samples the coarse `flowField` bilinearly at each tile pixel; computes hydraulic-geometry channel width (W ∝ Q^0.5, Lacey 1930) and burn depth (∝ discharge); stamps a parabolic cross-section groove so rivers carved at the coarse scale resolve into crisp valleys at high zoom instead of FBM-filled smears. Wired into `pyramidTile` via `opts.coarseFlow`; LOD panel "Burn river channels" toggle (`_lodBurnRivers`, default false → off → bit-identical to v0.093). Never touches the base `field`. 461 green (+6 burnChannels assertions: zero-flow no-op, high-flow depression, far-cell unchanged, never raises, floor limit, seam-Δ<1e-4). Phase 2 (per-tile micro-erosion with fixed boundary cells) and Phase 3 (delta distributary bifurcation) deferred. Browser check: toggle Burn channels → crisp carved river valleys at high LOD zoom; delta fans → multiple distributary channels.
- 2026-06: **v0.093 — debug legend fix** (user report: legend didn't always update on debug view switch). Added the 4 missing cases to `updateLegend()`: `oro` (divColor swatches — warm=uplift/mountain, cool=trench/graben), `geoid` (divColor swatches — warm=geoid bulge, cool=depression), `tides` ([235,170,30] → shelf / [20,70,150] → open ocean), `cbiome` (6 representative Cartalith biome swatches). All 16 debug views now update the legend immediately on click. UI-only; bit-identical to v0.092. 455 green.
- 2026-06: **v0.092 — LOD/sculpt/overlay bug-fix pass** (user reports). (1) Freehand sculpt gated to the Sculpt tab via `_activeTab` (Terrain/Style canvas drag no longer edits the heightmap). (2) `renderBiomeTileRGBA` smooths the tile sea floor + hillshades the smoothed bathymetry + resolution-independent grain → LOD/atlas ocean matches the main map (no more pixelated seas); raw tile still drives the land/water mask (crisp coasts). (3) Bolder chunk-debug grid + child-quadrant guides for a legible tiling preview. Audited headless: couplings (L1/L2/L3/L6), tiling (seam Δ=0, pyramid, atlas round-trip), save→merge round-trip for v0.086–0.092 params. Main-map render bit-identical to v0.091. 455 green. LOD ocean / overlays / sculpt-gating need a browser confirm.
- 2026-06: **v0.091 — L6 cryosphere ice-albedo feedback** (`docs/research/system-coupling-audit.md` §6). `state.climate.albedo` slider (default 0 ⇒ off ⇒ bit-identical). Pure `applyCryosphereAlbedo(temp,k)` runs a damped fixed-point T = base − k·9·ice(T) (ice = smoothstep(1,−6,T)) so ice cover cools further and grows — polar caps + high massifs broaden. Called at the tail of `computeTemperature`; GPU temp path bypassed when albedo>0. Off ⇒ bit-identical to v0.090 (field/temp/bakePixel byte-identical). 451 green. Albedo aesthetics need a browser pass.
- 2026-06: **v0.090 — T5 orogeny tuning + archetype hooks (tectonic graph T0–T5 complete)** (`docs/research/tectonic-feature-graph.md`). `buildOrogenyField` gains `opts.foldK` (collision fold-ripple, default 0.16) + `opts.trenchK` (trench depth, default 1.0) — omitting both reproduces the v0.089 kernel bit-exactly. `state.tect.foldIntensity`/`trenchDepth` sliders thread them through `generate()` + the Orog preview; `deriveFromWorldStructure()` turns on the graph and maps fold (`0.6+tectonicEnergy`) / trench (`0.7+0.8·oceanDepth`) from the active archetype. Per invariant 5, default WS-disabled world bit-identical; graph-on with default sliders also reproduces v0.089. 442 green.

- 2026-06: **v0.089 — R4 ridged-noise elevation-weighted relief** (`docs/research/terrain-rendering-enhancement.md` §2; **R-series R1–R4 complete**). Pure `ridgedFbm(x,y,oct,s)` (configurable-octave ridged multifractal; `ridgedFbm(…,6,…)` bit-identical to the legacy `ridged()`, which is left untouched so `amplifyRegion` stays byte-stable). Gated `state.viz.ridgedRelief` slider adds H²-weighted folded-crease shading in `landColorCore` at world coords (seamless across tiles). Off ⇒ bit-identical to v0.088. 436 green. Ridged-relief aesthetics need a browser pass.
- 2026-06: **v0.088 — R3 procedural texture synthesis + minor-channel flow lines** (`docs/research/terrain-rendering-enhancement.md` §7,§4). Two gated Style sliders (default 0 ⇒ bit-identical). `state.viz.texture` adds a three-frequency `fbm` colour modulation `C·(1+0.1·T)` inside `landColorCore` in world coords (seamless across tiles, bakes); `state.viz.minorStreams` draws faint blue-grey threads for low-accumulation cells below the trunk threshold (screen overlay in `surfaceColor`, like the trunk rivers). Off ⇒ bit-identical to v0.087 (field/temp/bakePixel byte-identical, deterministic-rain verified). 429 green. Texture/channel aesthetics need a browser pass.
- 2026-06: **v0.087 — R2 ridge crests + slope-material refinement** (`docs/research/terrain-rendering-enhancement.md` §3,§6). Two gated Style sliders (default 0 ⇒ bit-identical). `buildCrestField` (pure; convex ∩ steep via `G^1.5` slope weight, `sx,sy` coarse-scale so tiles match the main map) → `_crestField` thin bright sunlit-rock strokes via `applyCrest` in surfaceColor/bakePixel/tiles; `state.viz.rockSlope` adds a `G^1.5` rock recolour tint inside `landColorCore` (post-mix, leaves the Σ=1 weights untouched). Off ⇒ bit-identical to v0.086 (field/temp/bakePixel byte-identical, deterministic-rain verified). 423 green. Crest/rock aesthetics need a browser pass.
- 2026-06: **v0.086 — Atlas Phase 4: portable export/import** (`docs/ATLAS_ARCHITECTURE.md`). Pure `atlasChunkFile`/`buildAtlasManifest` + shim-tested `atlasExportEntries`/`atlasImportEntries` round-trip a world's baked chunks to a `World/` ZIP (rg16 height + visual PNG per chunk, gzip-optional, schema-1 manifest carrying worldKey + `serializeState()` params) and back into IndexedDB; `_atlasBaked` repopulates when the import matches the current world, else surfaces on the next matching generate via `atlasSyncWorld`. Export atlas… / Import atlas… buttons. Pure additions + UI ⇒ bit-identical to v0.085. 412 green (full IDB→ZIP→IDB round-trip, ≤1-LSB height). Download/file-picker need a browser pass.
- 2026-06: **v0.085 — unified sculpting brush** (user request: heightmap brushes belong only in the Sculpt tab). Deleted the weak 3-mode LOD `lodBrushSeg`/`_lodBrush`; upgraded `brushHeight` to the full 8-mode sculpt-quality kernel (`opts={nx,ny,centerH}`) driven by the shared `state.brush`; `editTileAt` resolves a per-stroke normal (cliff/ridge/canyon) and skips stamp-brush repeats (mesa/volcano) on drag. The **Edit tiles** checkbox moved Terrain → Sculpt ("Edit LOD tiles") so every heightmap-modifying control lives in one tab. `generate()` bit-identical to v0.084 (field+temp byte-identical). 400 green. LOD-edit interaction needs a browser pass.
- 2026-06: **v0.084 — R1 ambient occlusion** (`docs/research/terrain-rendering-enhancement.md` §5). `state.viz.ao` (default 0 ⇒ off ⇒ bit-identical). Pure `buildAOField`/`aoMul` darken cells sitting below their broad-blur neighbourhood (valleys/basins), ridges unchanged; `landColorCore` gained a trailing `ao` param (default 1); `surfaceColor`/`bakePixel` use `_aoField` (biome map only), `renderBiomeTileRGBA` computes local AO. Style → Ambient occlusion slider. Off ⇒ bit-identical to v0.083 (cmp-clean). 394 green. (The engine already had multi-scale hillshade; AO was the framework's genuine gap.) AO aesthetics need a browser pass.
- 2026-06: **v0.083 — Atlas Phase 3: biome-coloured tiles** (`docs/ATLAS_ARCHITECTURE.md`). `renderBiomeTileRGBA(tile,W,H,bounds)` reuses `landColorCore` (height/slope/hillshade from the tile, T/M/flow/aspect sampled from the coarse fields at the tile's world coords; slope rescaled to coarse-cell units). `drawLODView` picks the renderer by View mode (Biome vs Relief); `tilePngBytes` gained an optional `bounds` arg → atlas-bake + region-export PNGs store the biome visual. Default render untouched ⇒ bit-identical to v0.082. 387 green. LOD biome tiles need a browser pass.
- 2026-06: **v0.082 — Atlas Phase 2b: cross-session persistence + status + metadata** (`docs/ATLAS_ARCHITECTURE.md`). `atlasSyncWorld()` (fired from `generate()`/startup) queries IDB (`atlasKeysForWorld` via the `world` index) to repopulate `_atlasBaked` for the current `_worldKey` → bakes survive reload/regenerate; re-checks `wk===_worldKey` after each await. Per-world metadata record (`atlasMetaRec`/`atlasMetaKey`, no `worldKey` field so the index excludes it) → `#atlasStat` status line. `atlasClearWorld` refactored cursor-free. Test-only in-memory IDB shim (`__makeIDBShim`, not auto-installed → default suite/cmp stay no-IDB) drives a full headless round-trip. Off/no-IDB ⇒ bit-identical to v0.081. 381 green. IDB persistence/status UI need a browser pass.
- 2026-06: **v0.081 — Atlas Phase 2a: IndexedDB chunk baking + images-override** (`docs/ATLAS_ARCHITECTURE.md`). `_atlasBaked` stub wired to a real IDB store (`cartalith_atlas`/`atlas`); pure cores `worldKey`/`atlasKeyStr`/`atlasEncodeChunk`/`atlasDecodeChunk`/`bakedCover` (tested); browser-only `atlasOpen`/`Put`/`Get`/`ClearWorld`/`atlasLoadImg`/`bakeVisibleTiles` (feature-detected). `drawLODView` serves baked chunks; `refineVisibleTiles` skips baked-covered tiles; **Bake / Clear atlas** buttons. Off/no-IDB ⇒ bit-identical to v0.080. 369 green. IDB/canvas/bake flow need a browser pass.
- 2026-06: **terrain-rendering-enhancement research doc** (`docs/research/terrain-rendering-enhancement.md`, user framework): multi-scale shading, ridged-noise synthesis, slope-material masks, flow-field enhancement, AO, ridge crests, three-frequency texture synthesis; phased R1–R4 (all render-only/gated). Roadmap items added.
- 2026-06: **v0.080 — LOD interaction bug fix + UI tidy** (user regression report). Fixes the LOD-mode zoom and terrain-painting regression: normal sculpt `pointerdown`/`pointermove` now gate on `!_lodOn`; wheel and pinch-zoom route to continuous `_lodZoom` scaling in LOD mode; `lodChk` toggle defensively clears stuck `painting`/`panDrag`. Moves `showRivers`/`smoothRivers`/`sharpBiomes` from the Glacial erosion panel into the Style tab where map-visual toggles belong; fixes the stranded glacial hint. Handler/markup-only ⇒ bit-identical to v0.079. 357 green.
- 2026-06: **v0.079 — Atlas Phase 1: chunk model + debug** (`docs/ATLAS_ARCHITECTURE.md`). The LOD pyramid becomes a quadtree of chunks with lifecycle Unexplored→Generated→Edited→Baked (`chunkState`, atlas authoritative; `_atlasBaked` stub until Phase 2). Pure helpers `chunkParent`/`chunkChildren`/`chunkColorHash` (tested). Chunk-debug overlay on the LOD view (Grid/Colors/Labels toggles). Gated ⇒ default bit-identical to v0.078. 357 green.
- 2026-06: **v0.078 — Cartalith biome-paint PoC + sharper biomes** (user request). (1) The editor's 15-biome palette auto-filled from this tool's classification (`buildCartBiome`, `ELEV_TO_CART` + elevation overrides) as a debug-only **CBiome** view — a generated world hands the editor a ready paint grid. (2) `bioJitter` adds a finer ecotone octave (gated `state.viz.sharpBiomes`, default on) so biome detail sharpens with resolution; off ⇒ bit-identical to v0.077. 348 green. Kicks off the **Hierarchical Reverse-Refinement Atlas** workstream (IndexedDB-backed progressive baking; plan in /root).
- 2026-06: **v0.077 — river carving interplay** (second half of the river pass). `enforceChannelDescent` carves brushed rivers as monotonically descending channels (cuts through rises so they actually flow); the brush locks them into riverMask/riverFloor and `enforceRiverChannels` (in eroFinish/evolve/sediment) keeps deposition from refilling them. No locked rivers ⇒ no-op ⇒ bit-identical to v0.076. 342 green.
- 2026-06: **v0.076 — smooth discharge-widened rivers** (river quality pass, rendering first). `buildRiverField` replaces the blocky 1-cell flow-threshold overlay with rivers widened ∝ discharge (trunk wide, tributaries thin), blended on the biome map. Gated `state.viz.smoothRivers` (default on; legacy per-cell when off). Render-only: field/temp/rain bit-identical, ocean untouched, land river pixels improved. 336 green. Next: vector river strokes + brush/erosion carving interplay.
- 2026-06: **v0.075 — LOD per-tile editing (Stage 3)** (`docs/LOD_PYRAMID_PLAN.md`). Pure `brushHeight` (raise/lower/smooth) + `lodPick` (canvas→tile+local coords); edits persist in `_lodEdits` so the procedural base stays reproducible (drawLODView prefers them, Refine never overwrites); tile-aware Ctrl-Z undo. **Edit tiles** toggle + brush-mode seg. Off ⇒ bit-identical to v0.074. 332 green. Tiling workstream Stage 1–3 complete (export bake-back of edits = follow-up).
- 2026-06: **v0.074 — LOD overview-then-refine** (user request: not on-the-fly). The tiled view shows the coarse overview instantly (no procedural work); a **Refine detail** button amplifies the finer tiles for the current view on demand (cached); `drawLODView` overlays only already-generated tiles (zero work on navigate). Finetune the overview first, then refine where asked. Off ⇒ bit-identical to v0.073. 322 green.
- 2026-06: **v0.073 — tiled LOD viewer (Stage 2)** (`docs/LOD_PYRAMID_PLAN.md`). Engine (tested): LRU `_lodCache`, `tilesInView`, `collectVisibleTiles`. Compositor (browser): `drawLODView` renders only the visible region's pyramid tiles into the canvas with self-contained LOD zoom/pan (CSS transform identity so it never fights viewT) — dive into amplified detail without an 8K canvas. **Tiled LOD** toggle + 512/1K/2K/4K selector; off ⇒ renderNow bit-identical to v0.072. 317 green. Stage 3 = per-tile editing; compositor needs a browser.
- 2026-06: **v0.072 — LOD tile-pyramid core (Stage 1)** (`docs/LOD_PYRAMID_PLAN.md`, user request). Pure `pyramidTile`/`pyramidDims`/`pyramidTileBounds`/`pyramidLevelForZoom` — a 2^z×2^z quadtree over the coarse world built from the proven seamless `refineTile`/`amplifyRegion`, for a tiled LOD viewer that never holds an 8K canvas (8K crashed browsers). Headless-tested (seam Δ=0, addressing, determinism); generate() bit-identical to v0.071. 313 green. Stage 2 = live viewer + LRU cache (browser); Stage 3 = per-tile editing; export still bakes full 8K via bakeTiled.
- 2026-06: **v0.071 — resolution-switch NaN fix (root cause of "bad sea at 2K")**. `computeWarp`'s cache ignored resolution, so a res-switch button returned stale-sized warpX → NaN across most of the map (the height loop + heterogeneity read past warpX's end). One-line fix (cache also checks `warpX.length===GW*GH`). 512→1K→2K now 0 NaN (was up to 94%). Fresh generate bit-identical to v0.070. 304 assertions green.
- 2026-06: **v0.070 — G3 moons & tides** (`docs/research/gravity-influence.md`). `state.planet.tides` (default off ⇒ bit-identical). `tidalForcing`=Σ massRel/distRel³; `computeTideField` → spring tidal-RANGE field (forcing×k2÷g, amplified in shallow shelves / near coasts) marking intertidal flats + coastal hazard zones. Tides debug view + intertidal overlay + `tidal_range.f32` export. 300 assertions green (range ∝ mass, 1/dist³, 1/g, larger near coast). Completes the gravity workstream (G2 geoid + G3 tides; G4 tidal sedimentation deferred).
- 2026-06: **v0.069 — loop 3: mass-conserving sediment routing** (`docs/research/system-coupling-audit.md` §3). Pure `routeSediment` carries an eroded-sediment supply down the drainage network and deposits it (floodplains, deltas/shelves, sink pooling) conserving mass exactly (Σdeposit=Σsupply). Opt-in **Sediment fill** button (`depositSediment`: carve → route → redeposit). generate() bit-identical to v0.068. 289 assertions green. Completes the three coupling loops the user picked (L1/L2/L3) = roadmap item 4.
- 2026-06: **v0.068 — resolution-independent ocean grain + 4K/8K**: the water noise was per-cell frequency so it grained-up at 2K (user report: fine at 512/1K, pixelated at 2K). Now resolution-independent (×25.6/GW = old ×0.05 at 512). Added 4096/8192 working-resolution buttons (memory-heavy, hint added). Render-only, water-only; field/temp/rain bit-identical to v0.067, land byte-identical. 282 assertions green.
- 2026-06: **v0.067 — loop 2: ocean currents → winds/rainfall coupling** (`docs/research/system-coupling-audit.md` §2). `oceanSSTAnomaly` extracted and folded into the weather sim's sea temperature BEFORE `buildWind`, so warm/cold currents steer pressure, evaporation and rainfall (was a post-process tint). Gated on `state.climate.currents` (default off ⇒ bit-identical to v0.066). 282 assertions green (winds measurably respond to the injected anomaly).
- 2026-06: **v0.066 — loop 1: climate↔erosion coupled evolution** (`docs/research/system-coupling-audit.md`). `evolveCoupled(cycles)` (Erosion panel Evolve button): each cycle carves with stream-power, rebounds isostatically, recomputes drainage + weather (incl. rainfall) on the new terrain — rain shadows/rivers migrate with the orography. Opt-in op → generate() bit-identical to v0.065. 277 assertions green. First of the audit loops the user prioritised (L2 currents→winds, L3 sediment routing next).
- 2026-06: **v0.065 — sharper seas, part 2**: shade the SMOOTHED sea floor (`seaShadeFrom` → `_seaShade`) instead of the raw bumpy seabed (v0.063 only smoothed the depth, not the hillshade), and restore v0.015's shader constants now that the inputs are smooth. Reproduces the old flat/sharp seas while coasts stay crisp. Render-only, water-only: field/temp/rain bit-identical, land byte-identical. 271 assertions green.
- 2026-06: **v0.064 — T4 transform faults** (`docs/research/tectonic-feature-graph.md`, completes T0–T4): `buildOrogenyField` gains `opts.shear`; transform polylines (shear-dominant, were inert) now stamp a linear fault valley + a transpressional pressure ridge offset laterally by λ·S + an opposite releasing-bend depression, amplitude from |shear| not normal stress. Ridge reverses side with shear sign and its offset scales with |shear| (both asserted); no-shear ⇒ zero. 269 assertions green; off ⇒ bit-identical to v0.063. T0–T4 feature-complete; **T5** (archetype hooks + fold-intensity/trench-depth sliders) is the optional tuning pass.
- 2026-06: **v0.063 — smoothed-bathymetry water shading** (user bug report: seas read "blocky"/unsharp at the 2K default). Root cause: the water shader is byte-identical to v0.015, but the depth band + seabed hillshade resolve per-cell bathymetry noise that is far busier at 2048 than at the old 512 default. Fix (render-only): `smoothSeaH` (two box passes, radius ∝ resolution) flattens the sea floor into broad depth zones for water colour only — the land/water mask still uses the raw field, so coastlines stay crisp; grain + seabed hillshade also softened. Proven render-only: field/temp/rain bit-identical, land pixels byte-identical, only water changes. 262 assertions green.
- 2026-06: **v0.062 — T3 boundary-type features** (`docs/research/tectonic-feature-graph.md`): `buildOrogenyField` gains a `crust` input and stamps a per-type signed profile selected by the T0 matrix — collision (multi-ridge × fold ripple + plateau + foreland basin), subductionOC (trench + Andean arc), arcOO (trench + island arc + backarc basin), rift (axial graben + shoulders, on top of kept divergent stress); transform waits for T4. Trench side from a per-polyline majority vote over `crust` (oceanic = base<0), asserted to track/flip with the crust input. Amplitude stays linear in margin |stress|. 258 assertions green (incl. trench-on-ocean-side+flip, island-arc both signs, rift graben, foreland basin, gate round-trip). Off ⇒ bit-identical to v0.061. **Next: T4** transform offsets (shear-dominant segments laterally displace ridge axes; linear transform valleys).
- 2026-06: **v0.061 — T2 orogenic uplift kernel** (`docs/research/tectonic-feature-graph.md`): opt-in `state.tect.tectonicGraph` replaces convergent blurred-stress blobs with the multi-ridge kernel (`A·e^(−d²/σ₁²)+0.5A·e^(−(d−d₁)²/σ₂²)+0.3A·e^(−(d+d₁)²/σ₂²)`) stamped along T1 graph polylines via pure `buildOrogenyField` (signed distance, fbm crest wobble, amplitude = mean margin |stress|, max-combined). Divergent stress kept verbatim (rifts = T3, transforms = T4). Orog debug view previews the uplift gate-off. Off ⇒ bit-identical to v0.060 (cmp + in-suite round-trip). Acceptance: synthetic margin → ≥3 ridges, valley cols <75%, asymmetric flanks. 253 assertions green. **Next: T3** boundary-type features (trench/arc, fold belts, foreland, grabens).
- 2026-06: **v0.060 — T1 boundary polyline graph** (`docs/research/tectonic-feature-graph.md`): vectorises `boundaryMask` for the mountains-as-structures workstream. `thinMask` (Zhang–Suen 1-px skeleton) + `traceBoundaries` (chains between degree≠2 nodes, pure loops separate) → `{polylines:[{pts,length,closed,curvature}], nodes}`, both pure/testable. `currentBoundaryGraph()` tags each polyline with its dominant T0 `boundaryType` and caches (cleared per `generate()`, built lazily for the Tect view, which now overlays the stroked graph + junction dots). Data/overlay-only → render bit-identical to v0.059. World-wrap split documented as a refinement. 239 assertions green. **Next: T2** orogenic uplift kernel along these polylines (gated behind `state.tect.tectonicGraph`, off ⇒ bit-identical).
- 2026-06: **v0.059 — B2 texture splatting** (`docs/ASSET_PACK_FORMAT.md`, completes the asset tier): pack ground textures blend into the biome render. `state.viz.splat` slider (default 0.7) gated on a loaded texture pack → no-pack render bit-identical to v0.058. `landColorCore` post-mix block (procedural mix byte-untouched, `_splatK=0` skips): **tint-ratio splat** `tinted_c = tex_c·inv_c·rampCol_c` (texture detail re-coloured by the climate ramp so temp/moisture tinting survives), weighted by `materialWeights`, `col=mix(col,texCol/cov,_splatK·cov)`. Parchment slot uses pack paper when present. Bakes splat consistently (set in `buildGridFields`). 222 assertions green (land-only change, ocean untouched, 0-strength + no-pack both bit-identical). Texture aesthetics = browser check.
- 2026-06: **v0.058 — T0 tectonic shear field + boundary-type matrix** (`docs/research/tectonic-feature-graph.md`, first phase of the user-prioritised workstream): `computeStress` now also computes tangential shear `S=(vA−vB)·t` (`shearField`, blurred + |max|-normalized) and classifies every boundary cell via the pure `classifyBoundary(oceanA,oceanB,C,S)` matrix — shear-dominant→transform, convergent split by crust (C–C collision / O–C subduction / O–O island arc), divergent→rift; cells take their strongest-interacting pair. Frozen `BTYPE` order; `boundaryType` nonzero only on boundaries (asserted). Tect debug view + legend. Data-only → bit-identical to v0.057 (field/temp/rain/render cmp-clean). 218 assertions green (matrix table, synthetic head-on→collision + pure-shear→transform plate pairs, real-world ≥3 types). T1 boundary graph next.
- 2026-06: **v0.057 — Ocean-currents debug view + two-row debug picker** (user request): `currentOceanField()` (read-only, mirrors `applyOceanCurrents`: `buildWind` → wind-driven surface flow + warm/cold SST anomaly, masked to ocean) → per-pixel warm(orange)/cold(blue) SST map + coarse flow arrows over water. `#debugSeg` became a 6-col `.seg.grid` (12 views in two tidy rows). Render-only → bit-identical to v0.056; 205 assertions green.
- 2026-06: **v0.056 — Style tab + in-app asset-pack import** (`docs/ASSET_PACK_FORMAT.md`): a third sidebar tab gathers map styling (parchment/icons/waves moved from View) + a scale-bar toggle + an asset-pack Import/Clear UI with thumbnail inspector. Importer is pure-core + browser-shell: `parsePackCsv`/`parsePackManifest` (JSON-wins, frozen slot vocab, unknown→warnings), `pickIconVariant` (deterministic anti-repeat), `spriteDrawRect`, `finalizePackTexture`; `unzipAny` central-dir reader handles STORED **and** DEFLATED packs via `DecompressionStream`. `drawMapIcons` draws pack sprites when present, else procedural glyphs. `assets/make_sample_pack.py` → committed `assets/sample_pack.zip` (ZIP_STORED, 7 textures + 9 icons, CC0). `assetPack` is a module global (never serialized). 201 assertions green; field/temp/rain/render bit-identical to v0.055 (no pack ⇒ inert). B2 texture splatting = next (v0.057).
- 2026-06: **v0.055 — region export: cols×rows + tile resolution** (user request): the region-refine export now takes an explicit tile grid (`refCols`×`refRows`) and per-tile resolution instead of a fixed N×N square. New pure `tileDims(sel,cols,rows,ts)` scales tile pixels to each tile's true coarse aspect so the assembled image always keeps the selection's shape — non-square selections are no longer squished (algebraically aspect-exact; asserted). `refineTile`/`exportRegionTiles`/`tilePngBytes`/manifest carry non-square dims; seams stay Δ=0 for non-square tiles. 176 assertions green; generate()+render bit-identical to v0.054. Also: **asset-pack design** — `docs/ASSET_PACK_FORMAT.md` proposes in-app ZIP import (supersedes sibling-folder), awaiting user OK before building.
- 2026-06: **v0.054 — G2 geoid sea-level field** (`docs/research/gravity-influence.md`): opt-in `state.planet.geoid={enabled,amp}` makes sea level a *field* (`buildGeoid`: J2 rotational bulge `rotK·(cos²lat−2/3)` + low-degree harmonics + low-frequency mantle `fbm`, zero-mean, peak=amp; pure). A cell is ocean iff `field[i] < seaLevel + geoidField[i]`; `geoidField` null while off so consumers no-op. Local sea level threads through water mask, ocean SST, climate, erosion effective-height and the renderer; Geoid debug view + UI. Off ⇒ bit-identical to v0.053 (field + RGBA cmp-clean). 172 assertions green (J2 equator-bulge, zero-mean, peak=amp, determinism). GPU temp→CPU while geoid on; **G3** moons/tides next.
- 2026-06: **v0.053 — region-refine export (16k tiling browser wiring)**: drag-select a rectangle on the map (`normRegion` pure clamp; dashed overlay; exclusive with guide mode) → `exportRegionTiles()` bakes it tile-by-tile (`refineTile`→`packHeight16`→gzip via native `CompressionStream` with store fallback→optional hypso×shade PNG via OffscreenCanvas→schema-2 manifest with coarse bounds) into one ZIP. Binary path proven headless end-to-end (exported tile gunzip+unpack matches direct `refineTile` ≤1 LSB; gzip round-trip bit-exact). 158 assertions green; bit-identical to v0.052. Drag UX/PNG/16k memory = browser check.
- 2026-06: **v0.052 — tiling core** (`docs/WORLD_REGIONAL_TILING_PLAN.md` Stages 3–5): pure, headless-verifiable primitives on top of v0.044's `amplifyRegion`. `refineTile()` splits a coarse region into a cols×rows grid with one-cell shared-edge overlap → seam-Δ=0 across a full 3×2 split both axes (and tile(0,0) matches a direct `amplifyRegion`). `packHeight16`/`unpackHeight16` 16-bit R+G height packing (round-trip ≤1 LSB ≈7.6e-6, clamps), wired into `exportZip` (`heightmap_rg16.bin`) + `loadZip` fallback. `buildTileManifest()` manifest v2 (schema 2, worldSeed, per-tile coarse bounds, height encoding, compression) — superset of the old flat index, now emitted by `bakeTiled`. 145 assertions green; bit-identical to v0.051. Region-select UI / per-tile OffscreenCanvas+worker render / fflate / 16-bit PNG export = browser follow-up.
- 2026-06: **v0.051 — B4 coastal wave lines** (`docs/BIOME_AND_VISUALS_PLAN.md` Part B): `state.viz.waves` (default off → field AND rendered RGBA bit-identical to v0.050). `computeCoastDistance()` pure chamfer distance transform (land=0, ocean=distance-in-cells) → concentric foam contours that brighten near shore and fade into deep water, modulating WATER cells only (asserted: land pixels stay byte-identical on toggle). 133 assertions green. Vector spline coastlines deferred (optional). Wave look = browser check.
- 2026-06: **v0.050 — B1 parchment + B3 stylized icon layer** (`docs/BIOME_AND_VISUALS_PLAN.md` Part B, zero-asset tier): `state.viz={parchment,icons}` (both default off → field AND rendered pixels proven bit-identical to v0.049). Parchment = two-octave paper-fibre grain multiplied into the biome/relief modes + warm tint, slider 0–1. Icon layer = `placeMapIcons()` (pure primitive: land-relative elevation thresholds 0.58/0.53, greedy largest-first spacing via grid buckets — Nortantis algorithm studied, no AGPL code copied; forest stipple from the frozen biome raster on closed-canopy classes) + `drawMapIcons()` procedural vector glyphs (peaked mountains with shaded east flank, hill arcs, conifer/broadleaf trees), painter-ordered north→south. Render-only; bakes/exports unchanged. 126 assertions green. Glyph aesthetics = browser check.
- 2026-06: **v0.049 — W0b worker stream-power + glacial carve** (`docs/research/engine-optimization.md`): both ops refactored into self-contained `streamPowerKernel`/`glacialKernel` (inline MinHeap + priority-flood routing, all inputs via args) shipped to blob-URL Workers via a generic `runErosionWorker` runner (progress %, sync fallback, shared `_eroBusy` lock); main-thread `eroFinish` (rebound→flow→climate→render) stays off-worker. Invariant 11 (self-containment) now covers all three kernels — rebuilt-from-`toString` bit-identical asserted for each. Proven bit-identical to v0.047 (seed 12345, 256px: stream + glacial outputs `cmp`-clean). 113 assertions green. Worker paths = browser check.
- 2026-06: **v0.048 — plotline feature brushes + pan/zoom UX** (`docs/research/map-painting-ux.md`): waypoint polyline sculpt (and its GPU shader) replaced by freehand guide strokes (RDP-simplified, Catmull-Rom smoothed) + `applyFeatureAlongCurve()` — a pure testable distance-field stamp synthesizing 7 features (mountain range, hills, ridge, plateau, river, canyon, escarpment) with fractal detail along the line. Shared `viewT` pan/zoom: mobile keeps its zoom buttons (+ pan toggle + pinch), desktop gains wheel-zoom-to-cursor + middle/space-drag pan. Dynamic km scale bar; Ctrl/Cmd-Z undo. 105 assertions green; generate() proven bit-identical to v0.047. Gestures/scale-bar/guide-preview = browser check.
- 2026-06: **v0.047 — Wind debug view**: `currentWindField()` + a `Wind` debug overlay (per-pixel hue=bearing/brightness=speed map + coarse arrow glyphs) makes the W1 planetary wind field visible. Read-only; bit-identical to v0.046. 87 assertions green (incl. tropics-easterly↔mid-latitude-westerly reversal). Arrow legibility = browser check.
- 2026-06: **v0.046 — stream-power fix + menu cascade** (user bug report): relief inversion (ridges-for-rivers) and 45° line artefacts fixed via MFD drainage (Freeman 1991), steepest-descent receivers, anti-ridge deposition clamp, normalised uplift defaulting to 0 (carve-only). Old solver proven to net-RAISE channels (−0.0028), new net-incises (+0.0023); 84 assertions green incl. valleys-not-ridges regression. Sidebar reordered to the planetary-formation cascade (Planet/Calibrate before structure/climate/erosion; Save/Performance last).
- 2026-06: **v0.045 — W3.5 ocean currents**: opt-in `climate.currents`; wind-driven surface currents transport heat meridionally (poleward→warm SST→mild wet coast; equatorward→cold SST→fog-dry coast). Cold-current cooler/drier-coast (Benguela/Atacama) and warm-coast (Gulf-Stream) signatures both verified; 82 assertions green; currents-off bit-identical to v0.044.
- 2026-06: **v0.044 — region amplification primitive** (`WORLD_REGIONAL_TILING_PLAN` Stage 3): `amplifyRegion()` pure/worker-ready — coarse upsample + world-space fBm detail tapered by relief, faded underwater. Adjacent tiles proven seamless (seam Δ=0), deterministic, constraint-preserving. 78 assertions green; generate() bit-identical to v0.043. Verifiable core of the 16k tiling pipeline (export/OffscreenCanvas/fflate = browser follow-up).
- 2026-06: **v0.043 — W3 seasons + Köppen**: opt-in `climate.seasons`; `simulateWeather`/`buildWind` take a solar-declination arg so summer/winter passes shift the thermal equator by axial tilt; `computeSeasons()` builds seasonal temp/precip + a `koppenField` via a full Köppen–Geiger classifier (30 frozen codes, normalized-rain→mm scale). Köppen debug view + raster/manifest export. 73 assertions green (incl. one world → 22 distinct climates); seasons-off bit-identical to v0.042.
- 2026-06: **v0.042 — biome raster handoff (A1)**: `buildBiomeRaster()` + frozen `BIOME_INDEX`/`BIOME_KEYS` (0=ocean, 1..12 ice…tropWet); `exportZip` now ships `biome_raster.bin` + `biome_index.json` manifest for Cartalith. Generate output bit-identical to v0.041; 63 assertions green. First brick of the dual-layer biome bridge.
- 2026-06: **v0.041 — W0 worker droplet erosion**: droplet pass refactored into self-contained `dropletKernel` (no module globals), stringified into a blob-URL Web Worker with progress events and sync fallback; field copied in / transferred back so the live heightmap never detaches. Kernel proven bit-identical to v0.040 (fixed-seed 20k-droplet cross-version diff) and self-containment is regression-tested by rebuilding from `toString()` with shadowed globals. 57 assertions green. Worker path needs one manual browser check.
- 2026-06: **v0.040 — W2 moisture physics**: bulk-aerodynamic ocean evaporation `E = Ce·U·(qs−q)` (wind-speed + saturation-deficit, `climate.bulkEvap`); ITCZ/dry-belt corrector parametrised as `climate.zonalK`, default halved to 0.5 after measuring that W1 bands already produce emergent equator-max / 25–40° dry-dip / wet-westerlies structure. Legacy saves (zonalK 1, bulkEvap off) bit-identical to v0.039. 53 assertions green incl. emergent zonal-structure regression test.
- 2026-06: **v0.039 — W1 planetary wind field**: per-cell winds = latitude-band circulation (cell count from day length/size/gravity, Earth=3) + Coriolis-deflected thermal pressure-gradient flow (geostrophic poleward of ~15°, downgradient near equator, `pressK` slider). Region mode gets banded winds by default; manual windDir kept as override; legacy saves load as manual/pressK=0 — proven bit-identical to v0.038. Per-cell upwind border inflow. 50 assertions green.
- 2026-06: **v0.038 — G1 planet parameters**: `state.planet {g, rotationHours, axialTiltDeg, radiusRel}` + Planet UI section. Gravity scales stream-power/droplet/glacial erosion (×g), lapse (×g, CPU+GPU), craters (×g^-0.22), waves (×1/g); peak altitude rescales ~1/g on slider change. Earth defaults proven bit-identical to v0.037 (fixed-seed cross-version diff). 42 assertions green.
- 2026-06: **v0.037 — natural-order pipeline fixes** (`docs/research/pipeline-order-audit.md`): generate() now runs flow(area) → climate → flow(discharge); `computeFlow(true)` seeds runoff from rainField (Whipple & Tucker 1999); droplet erosion spawns ∝ precipitation; `isostaticRebound()` after fluvial/glacial/droplet erosion (England & Molnar 1990, ~80% broad-unload rebound). 38 smoke assertions green.
- 2026-06: Repo scaffolded — CLAUDE.md, headless verification harness, `verify-elevation` skill, research docs (weather v2, gravity, optimization, unified UI, pipeline-order audit), unified tool plan.
